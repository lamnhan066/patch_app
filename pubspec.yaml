name: patch_app
description: "Lightweight helper for Shorebird Code Push: detects and applies updates, then restarts the app with confirmation."
version: 0.4.0
homepage: https://github.com/lamnhan066/patch_app

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  restart_app: ^1.8.3
  shorebird_code_push: ^2.0.6
  terminate_restart: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

platforms:
  web:
  android:
  ios:
  macos:
  windows:
  linux:
